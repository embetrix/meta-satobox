# /etc/sysctl.d/90-security.conf
# Kernel runtime hardening for headless Bitcoin/Tor node

# Restrict kernel pointer leaks
kernel.kptr_restrict = 2

# Restrict dmesg to root only
kernel.dmesg_restrict = 1

# Disable kernel profiling for unprivileged users
kernel.perf_event_paranoid = 3

# Restrict eBPF JIT to root
kernel.unprivileged_bpf_disabled = 1
net.core.bpf_jit_harden = 2

# Restrict ptrace to direct parent only (Yama LSM)
kernel.yama.ptrace_scope = 2

# Disable SysRq
kernel.sysrq = 0

# Restrict userns cloning (reduces container-escape attack surface)
kernel.unprivileged_userns_clone = 0

# Limit PID reuse to make pid-based attacks harder
kernel.pid_max = 65536

# Full address space randomization
kernel.randomize_va_space = 2

# Restrict access to kernel logs via /proc
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# Disable core dumps
fs.suid_dumpable = 0

# Disable IP forwarding
net.ipv4.ip_forward = 0

# Enable SYN cookies (anti-SYN-flood)
net.ipv4.tcp_syncookies = 1

# Ignore ICMP redirects (MITM protection)
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv4.conf.all.secure_redirects = 0
net.ipv4.conf.default.secure_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# Ignore source-routed packets
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0

# Enable reverse path filtering (anti-spoofing)
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# Log martian (impossible) packets
net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1

# Ignore ICMP echo broadcasts (Smurf attack mitigation)
net.ipv4.icmp_echo_ignore_broadcasts = 1

# Ignore bogus ICMP error responses
net.ipv4.icmp_ignore_bogus_error_responses = 1

# Harden TCP against TIME-WAIT assassination
net.ipv4.tcp_rfc1337 = 1

# Disable TCP timestamps (fingerprinting reduction)
net.ipv4.tcp_timestamps = 0

# Disable IPv6 forwarding
net.ipv6.conf.all.forwarding = 0

# Ignore ICMP redirects
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0

# Ignore source-routed packets
net.ipv6.conf.all.accept_source_route = 0
net.ipv6.conf.default.accept_source_route = 0

# Ignore router advertisements (static network config only)
net.ipv6.conf.all.accept_ra = 0
net.ipv6.conf.default.accept_ra = 0
